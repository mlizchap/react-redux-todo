// STORE
    // action creator is dispatched with no todos - no todos renders
    // action is dispatched with multiple todos - todos render to screen 

// ACTION CREATOR when the user clicks submit 
    // no text - does not submit, no action creator called 
    // repeat todo - error message appears, no action creator called
    // unique todo - createTodo action creator is called 

// STATE PROPS 
    // UserList has access to todo state props

// ACTION CREATOR PROPS
    // UserForm has access to action creator 

// REDUCERS 
    // correct state is returned depending on if action is passed 

xit('', () => {});